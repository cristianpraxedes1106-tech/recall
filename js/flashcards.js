const deck=getDeck(qs('id'))||getDecks()[0], cards=shuffle(deck.cards), state={i:0};$('#title').textContent=deck.title;function show(){const c=cards[state.i];$('#question').textContent=c.question;$('#answer').textContent=c.answer;$('#count').textContent=`${state.i+1} / ${cards.length}`;$('#bar').style.width=`${((state.i+1)/cards.length)*100}%`;$('#card').classList.remove('flipped')}show();$('#card').addEventListener('click',()=>$('#card').classList.toggle('flipped'));$('#next').addEventListener('click',()=>{state.i=(state.i+1)%cards.length;show()});$('#prev').addEventListener('click',()=>{state.i=(state.i-1+cards.length)%cards.length;show()});document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')$('#next').click();if(e.key==='ArrowLeft')$('#prev').click();if(e.key===' ')$('#card').click()});