const deck=getDeck(qs('id'))||getDecks()[0];let cards=shuffle(deck.cards),state={i:0};$('#title').textContent=deck.title;function show(){const c=cards[state.i];$('#question').textContent=c.question;$('#answer').textContent=c.answer;$('#count').textContent=`${state.i+1} de ${cards.length}`;$('#bar').style.width=`${((state.i+1)/cards.length)*100}%`;$('#card').classList.remove('flipped')}function next(){state.i=(state.i+1)%cards.length;show()}function prev(){state.i=(state.i-1+cards.length)%cards.length;show()}$('#card').addEventListener('click',()=>$('#card').classList.toggle('flipped'));$('#next').addEventListener('click',next);$('#prev').addEventListener('click',prev);$('#shuffle').addEventListener('click',()=>{cards=shuffle(cards);state.i=0;show();beep('click')});document.addEventListener('keydown',e=>{if(e.target.matches('input,textarea'))return;if(e.key==='ArrowRight')next();if(e.key==='ArrowLeft')prev();if(e.code==='Space'){e.preventDefault();$('#card').classList.toggle('flipped')}});show();