const canvas=document.getElementById('particles');if(canvas){const x=canvas.getContext('2d');let ps=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight}resize();addEventListener('resize',resize);for(let i=0;i<55;i++)ps.push({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.8+.4,v:(Math.random()-.5)*.18});(function loop(){x.clearRect(0,0,canvas.width,canvas.height);x.fillStyle='rgba(196,181,253,.6)';for(const p of ps){p.y-=p.v;if(p.y<0)p.y=innerHeight;x.beginPath();x.arc(p.x,p.y,p.r,0,Math.PI*2);x.fill()}requestAnimationFrame(loop)})()}function confetti(){for(let i=0;i<24;i++){const s=document.createElement('i');s.textContent='✦';s.style.cssText=`position:fixed;left:${Math.random()*100}%;top:45%;font-size:${10+Math.random()*18}px;z-index:99;pointer-events:none;animation:fall ${.6+Math.random()*.8}s ease-out forwards`;document.body.appendChild(s);setTimeout(()=>s.remove(),1500)}}