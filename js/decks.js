function deckCard(d){return `<article class="deck-card"><div class="deck-icon"><i class="bi bi-collection"></i></div><h3>${esc(d.title)}</h3><p>${esc(d.description||'Sem descrição')}</p><div class="deck-meta"><span>${d.cards.length} cards</span><span>RECALL</span></div><div class="deck-actions"><a class="small-btn" href="pages/study.html?id=${encodeURIComponent(d.id)}">Estudar</a><a class="small-btn" href="pages/quiz.html?id=${encodeURIComponent(d.id)}">Quiz</a></div></article>`}function renderDecks(target,limit){const el=typeof target==='string'?$(target):target;if(!el)return;const decks=getDecks().slice(0,limit||99);el.innerHTML=decks.map(deckCard).join('')||'<div class="panel">Nenhum deck ainda. Crie o primeiro!</div>'}