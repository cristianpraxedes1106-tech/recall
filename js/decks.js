function deckCard(d){const base=location.pathname.includes('/pages/')?'':'pages/';return `<article class="deck-card"><div class="deck-icon"><i class="bi bi-collection"></i></div><h3>${esc(d.title)}</h3><p>${esc(d.description||'Sem descrição')}</p><div class="deck-meta"><span>${d.cards.length} cards</span><span>RECALL</span></div><div class="deck-actions"><a class="small-btn primary" href="${base}study.html?id=${encodeURIComponent(d.id)}">Flashcards</a><a class="small-btn" href="${base}deck.html?id=${encodeURIComponent(d.id)}">Ver deck</a></div></article>`}function renderDecks(target,limit){const el=typeof target==='string'?$(target):target;if(!el)return;const decks=getDecks().slice(0,limit||99);el.innerHTML=decks.map(deckCard).join('')||'<div class="empty-state">Nenhum deck ainda.<br><a class="text-link" href="pages/create.html">Crie o primeiro!</a></div>'}